#include "main.h"

/**
 * _abs - prints absolute value of integer n
 * Return: always 0 (success)
 */

int _abs(int n)
{
	unsigned char myChar;

	myChar = unsigned int(n);

	_putchar(myChar);
	return (0);
}
